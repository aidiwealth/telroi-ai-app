; Auto-generated by Telroi carrier provisioning — Ruach
[carrier-ruach-out]
exten => _81234XXXXXXXXXX,1,NoOp(Ruach 81: ${EXTEN:2})
 same => n,Set(CID=${ODBC_CALLERID_FOR(${CHANNEL(endpoint)},ruach)})
 same => n,GotoIf($["${CID}"=""]?nodid)
 same => n,Set(CALLERID(num)=${CID})
 same => n,Set(CALLERID(name)=Telroi)
 same => n,Dial(PJSIP/ruach-endpoint/sip:${EXTEN:2}@sip.ruach.ng:5060,60)
 same => n,Hangup()
 same => n(nodid),NoOp(Rejected: caller owns no DID on ruach — buy/select a DID or contact support)
 same => n,Hangup(21)

[from-ruach]
exten => _X.,1,NoOp(Inbound from Ruach -> DID ${EXTEN} from ${CALLERID(num)})
 same => n,Stasis(telroi,${EXTEN})
 same => n,Hangup()

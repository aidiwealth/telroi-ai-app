; Auto-generated by Telroi carrier provisioning — Kasooko
[carrier-kasooko-out]
exten => _82234XXXXXXXXXX,1,NoOp(Kasooko 82: ${EXTEN:2})
 same => n,Set(CID=${ODBC_CALLERID_FOR(${CHANNEL(endpoint)},kasooko)})
 same => n,GotoIf($["${CID}"=""]?nodid)
 same => n,Set(CALLERID(num)=${CID})
 same => n,Set(CALLERID(name)=Telroi)
 same => n,Dial(PJSIP/kasooko-endpoint/sip:${EXTEN:2}@96.0.46.249:5060,60)
 same => n,Hangup()
 same => n(nodid),NoOp(Rejected: caller owns no DID on kasooko — buy/select a DID or contact support)
 same => n,Hangup(21)

[from-kasooko]
exten => _X.,1,NoOp(Inbound from Kasooko -> DID ${EXTEN} from ${CALLERID(num)})
 same => n,Stasis(telroi,${EXTEN})
 same => n,Hangup()
